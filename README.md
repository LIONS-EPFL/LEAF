# LEAF

In src, we include dependencies for this project.

Run `train_AT_text_only.py` for training, `generate_text_to_image.py` for text to image generation and  `eval_zero_zhot_text.py` for zero shot text classification.
